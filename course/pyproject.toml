[project]
name = "course"
version = "0.1.0"
description = "Personal AI Assistant"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-frontmatter>=1.1.0",
    "requests>=2.33.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
